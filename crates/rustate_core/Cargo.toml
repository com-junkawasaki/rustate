[package]
name = "rustate_core"
version = "0.1.0"
edition = "2021"

[dependencies]
# クレート固有の依存関係 (必要に応じて追加)
tokio = { workspace = true }
serde = { workspace = true }
async-trait = "0.1"
thiserror = "1.0"
uuid = { workspace = true, features = ["v4"] }

[dev-dependencies]
# クレート固有の開発用依存関係 