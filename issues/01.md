名前の重複エラー (E0255): state_machine.rs の末尾にある pub use self::{...} で、モジュール内で定義した型 (Context, Event, State, 各 *Client トレイト) を再度エクスポートしようとしているため、名前が衝突しています。
インポート解決エラー (E0432):
rustate クレートの rustate_action などのマクロや型が見つからない。これは rustate = "0.1.0" にダウングレードしたため、API が変更されたか、必要な feature が有効になっていない可能性があります。
lib.rs で state_machine::Error をインポートしようとしているが、存在しない。
プライベートアイテムエラー (E0603): lib.rs で state_machine::StateMachine をインポートしようとしているが、state_machine.rs で StateMachine (元は CoordinatorMachine) が pub(crate) で定義されているため、クレート外 (この場合は lib.rs) からアクセスできない。
所有権エラー (E0382): アクション関数内の tokio::spawn で、job_id_clone を match アーム内でイベントに move した後、後続の error! マクロで再度使用しようとしているため、move 後の借用エラーが発生しています。
Debug トレイト未実装エラー (E0277): Context 構造体に #[derive(Debug)] を付けていますが、フィールド内の dyn *Client トレイトオブジェクト (Arc<Mutex<dyn ...>>) が Debug を実装していないため、コンパイルできません。
型不一致エラー (E0308): match_job_id_response ガード関数が bool を返しているが、期待される型は StateMachineResult<bool> (つまり Result<bool, rustate::Error>) です。ガード関数は Result を返す必要があります。
メソッド未定義エラー (E0599): テストコード内で app_manager.get_event_sender() を呼び出していますが、AppManager に get_event_sender メソッドが（まだ）実装されていません。
その他: 未使用のインポート (warn(unused_imports)) や未使用の可変変数 (warn(unused_mut)) の警告。
修正方針:
優先度の高いものから順番に修正していきます。
rustate バージョン/インポート: rustate = "0.1.0" で state_machine! マクロなどがどのように提供されているか確認します。Cargo.toml で必要な feature を有効にする必要があるかもしれません。または、元の 0.5 に戻すことも検討します。（バージョン 0.1.0 はかなり古い可能性があります）
名前衝突 (E0255): state_machine.rs 末尾の pub use self::{...} を修正し、重複するエクスポートを削除します。StateMachineError など、必要な型だけを正しくエクスポートします。
プライベートアイテム (E0603): state_machine.rs 内の CoordinatorMachine (エイリアス StateMachine) の定義を pub(crate) から pub に変更します。
所有権 (E0382): tokio::spawn 内で job_id_clone を match アームに move する前に、ログ出力などで必要な場合はさらにクローンします。
Debug (E0277): Context 構造体の #[derive(Debug)] を一旦削除するか、各クライアントフィールドを Debug を実装するラッパー型で包むなどの対応が必要です。簡単のため、まずは Debug の derive を削除します。
型不一致 (E0308): match_job_id_response 関数の戻り値を Ok(...) でラップします。
メソッド未定義 (E0599): lib.rs の AppManager に pub async fn get_event_sender(&self) -> mpsc::Sender<StateMachineEvent> メソッドを実装します。